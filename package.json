{
  "name": "engram",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "lint:workspaces": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "cli": "npm run build --workspace @interwebalchemy/engram-cli --if-present && node packages/cli/dist/index.js",
    "prepare": "husky",
    "setup:hooks": "husky",
    "sync:agents": "node scripts/sync-agent-instructions.mjs",
    "sync:agents:check": "node scripts/sync-agent-instructions.mjs --check",
    "setup": "npm run prepare && bash scripts/setup-dev.sh",
    "dev": "bash scripts/dev.sh",
    "dev:build": "bash scripts/dev.sh --build",
    "dev:clean": "bash scripts/dev.sh --clean",
    "dream": "npm run build --workspace @interwebalchemy/engram-snapshot --if-present && npm run build --workspace @interwebalchemy/engram-dreams --if-present && node packages/dreams/dist/index.js",
    "dream:cleanup": "npm run build --workspace @interwebalchemy/engram-snapshot --if-present && npm run build --workspace @interwebalchemy/engram-core --if-present && npm run build --workspace @interwebalchemy/engram-dreams --if-present && node packages/dreams/dist/index.js --cleanup-only",
    "snapshot": "npm run build --workspace @interwebalchemy/engram-snapshot --if-present && node packages/snapshot/dist/index.js create",
    "snapshot:list": "npm run build --workspace @interwebalchemy/engram-snapshot --if-present && node packages/snapshot/dist/index.js list",
    "snapshot:restore": "npm run build --workspace @interwebalchemy/engram-snapshot --if-present && node packages/snapshot/dist/index.js restore",
    "serve": "bash scripts/serve.sh"
  },
  "license": "GPL-3.0-only",
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "eslint": "^9.39.4",
    "eslint-config-love": "^152.0.0",
    "husky": "^9.1.7",
    "typescript": "^5.4.5"
  }
}
