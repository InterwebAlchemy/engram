# Sample Capture

This is an example global inbox note — it surfaces automatically at session start regardless of which Thread is active.

Replace or delete this note once you've verified the inbox flow works.
